#ifndef CONFIG_HH
#define CONFIG_HH

#include <cstddef>
#include <cmath>

const size_t WIDTH = 1366;
const size_t HEIGHT = 768;
const size_t TARGET_FPS = 60;
extern double BOUNDARY;
extern double DTIME;

#endif
